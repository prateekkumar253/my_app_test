version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  pre_build:
    commands:
    - apt-get install -y telnet iproute2 net-tools
    - telnet 54.166.41.218 22
  build:
    commands:
      - echo "Done."
